import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.innovativenovels.preview',
  appName: 'Innovative Novels',
  webDir: 'dist'
};

export default config;
